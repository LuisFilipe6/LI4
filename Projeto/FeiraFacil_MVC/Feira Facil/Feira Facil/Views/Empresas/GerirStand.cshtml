@model IEnumerable<Feira_Facil.Models.Stand>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.idVendedor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.idCertame)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.idVendedor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.idCertame)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdStand">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdStand">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdStand">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
